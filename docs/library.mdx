---
title: Library
description: Wallet::from_mnemonic and chain Deriver.
---

```rust
use kobe::prelude::*;
use kobe::evm::Deriver;

let wallet = Wallet::from_mnemonic(
    "abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about",
    None,
)?;
let account = Deriver::new(&wallet).derive(0)?;
println!("path={} address={}", account.path(), account.address());
```

`Wallet::from_mnemonic` lives in `kobe-primitives`. Chain types are feature-gated re-exports on the `kobe` umbrella crate (`kobe::evm`, `kobe::btc`, …).
